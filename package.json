{
  "author": "Mark Cavage <mark.cavage@joyent.com>",
  "name": "sdc-clients",
  "description": "Contains node.js client libraries for SDC REST APIs.",
  "version": "6.1.0",
  "homepage": "http://www.joyent.com",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git@git.joyent.com:node-sdc-clients.git"
  },
  "main": "lib/index.js",
  "dependencies": {
    "lru-cache": ">=1.0.2",
    "nopt": ">=1.0.5",
    "restify": ">=0.3.5",
    "sprintf": ">=0.1.1",
    "ssh-agent": ">=0.1.0"
  },
  "devDependencies": {
    "node-uuid": ">=1.1.0",
    "jshint": ">=0.2.1",
    "whiskey": ">=0.3.3"
  },
  "scripts": {
    "pretest": "./node_modules/.bin/jshint lib tst && which gjslint; if [[ \"$?\" = 0 ]] ; then  gjslint --nojsdoc -r lib; else echo \"Missing gjslint. Skipping lint\"; fi",
    "test": "NO_PROVISION_TESTS=1 ./node_modules/.bin/whiskey --timeout 30000 --concurrency 1 -t \"`find tst -name *.test.js | sort -r | xargs`\""
  },
  "engines": {
    "node": "0.4"
  }
}
